# NEEPU CTF 项目约定

> 适用对象：开发者、维护者、AI Agent（Cursor / Copilot 等）  
> 原则：**先读约定与文档，再改代码；改完同步文档，再推送。**

---

## 一、每次开工前必读（按顺序）

| 顺序 | 文件 | 何时必读 |
|------|------|----------|
| 1 | **`项目约定.md`**（本文件） | 任何任务 |
| 2 | **`docs/功能说明书.md`** | 涉及功能、接口、页面、业务流程 |
| 3 | **`PUSH_CHANGELOG.md`** | 了解近期已改什么，避免重复或冲突 |
| 4 | **`AGENT.md`** | 前端 UI/UX、样式、组件结构 |
| 5 | **`docs/ui-style.md`** + **`docs/user-aesthetic-profile.md`** | 仅 UI/视觉改动 |
| 6 | **`docs/deploy-single-server.md`** | 部署、运维、环境变量 |
| 7 | **`docs/route-architecture-topology.md`** | 路由迁移、API 主路径与兼容路径 |

**Agent / 自动化协作者：** 接到任务后，**不得跳过第 1～3 步**；若任务涉及界面，必须再读第 4～5 步。

---

## 二、代码仓库结构约定

```text
neepu/                          ← Git 根目录（推送以此为准）
├── backend/                    ← Flask 后端（唯一权威后端目录）
│   ├── route/                  ← HTTP 路由，只做参数校验与响应
│   ├── services/               ← 业务逻辑
│   ├── server/                 ← 配置、ORM、邮件、容器访问
│   └── tests/                  ← 回归测试
├── frontend/                   ← Vue 3 前端
│   └── src/
│       ├── router/             ← 路由
│       ├── services/           ← API 封装（新接口优先放这里）
│       └── components/         ← 页面与组件
├── docs/                       ← 项目文档
├── PUSH_CHANGELOG.md           ← 每次推送的变更摘要
├── 项目约定.md                 ← 本文件
└── AGENT.md                    ← Agent 开发规范
```

- **不要**向仓库提交：`neepu/` 重复目录、`neepu.zip`、`.env`、`scripts/_*.py` 等临时脚本。
- **不要**提交任何密钥、口令、真实邮箱、内网 IP（示例用占位符）。

---

## 三、开发约定

### 3.1 后端

| 约定 | 说明 |
|------|------|
| 路由 | 新接口优先挂在已有主蓝图：`/api/competitions`、`/api/challenges`；避免复活 `/api/games` 等 410 路径 |
| 业务逻辑 | 写在 `backend/services/`，路由函数保持薄 |
| 容器 | 统一经 `container_service.py`，禁止在 route 里直接调 Docker SDK |
| Flag | 动态模板经 `flag_generator.normalize_flag_template()`；提交经 `submit_lock` + `FlagValidationService` |
| 时间 | 管理端时间用 `time_utils.parse_admin_datetime()`；开赛判断用 `game_has_started()` |
| 响应格式 | `{"code": 200, "msg": "...", "data": {...}}` |

### 3.2 前端

| 约定 | 说明 |
|------|------|
| API | 新接口优先写入 `frontend/src/services/`，减少组件内硬编码 URL |
| 组件 | 基础 UI → `components/ui/`；通用 → `components/shared/`；业务页 → `components/` |
| 做题核心 | `ChallengeWorkspace.vue` 被赛事与训练场复用，改前先读功能说明书 §3.4 |
| 构建 | UI 改动后执行 `cd frontend && npm run build` 验证 |

### 3.3 测试

- 逻辑修复在 `backend/tests/` 补充或更新回归测试。
- 推送前在根目录执行：`python -m pytest backend/tests/ -v`（如有对应用例）。

---

## 四、文档维护约定（与代码同步）

**凡属以下变更，必须在同一批提交中更新文档：**

| 变更类型 | 必须更新 |
|----------|----------|
| 新增/删除/修改 API 路由或 handler | `docs/功能说明书.md` 对应章节 + 附录索引 |
| 新增/删除 service 公共方法 | `docs/功能说明书.md` § 附录 services |
| 新增/修改前端页面或核心函数 | `docs/功能说明书.md` 对应前端表格 |
| 任何准备 push 的代码改动 | **`PUSH_CHANGELOG.md` 顶部新增一节** |
| 变更协作流程或目录规范 | **`项目约定.md`** |
| 变更 UI 规范 | `AGENT.md` / `docs/ui-style.md` |

### `PUSH_CHANGELOG.md` 每节模板

```markdown
## YYYY-MM-DD — 简短标题

**推送目标：** `https://github.com/Aln1lam/nee3.git`  
**分支：** `main`

### 变更摘要
- …

### 涉及文件
- …

### 部署/验证提示
- …
```

### `docs/功能说明书.md` 更新要点

- 写清：**文件路径 → 函数名 → 作用**。
- 新 API 补进对应功能域表格与 § 附录 route 索引。
- 统计数字（接口数、组件数）有显著变化时更新 § 二。

---

## 五、Git 推送约定

| 项 | 约定 |
|----|------|
| 远程仓库 | **`nee3`** → `https://github.com/Aln1lam/nee3.git`（主推送目标） |
| 分支 | `main` |
| 提交前 | `git status` 确认无 `.env`、密钥、zip、临时脚本 |
| 提交后 | 更新 `PUSH_CHANGELOG.md`，再 `git push nee3 main` |
| 提交说明 | 英文或中文均可，一句话说清「为什么改」 |

```powershell
# 示例流程（PowerShell）
git add <相关文件> PUSH_CHANGELOG.md docs/功能说明书.md
git commit -m "简述变更原因"
git push nee3 main
```

---

## 六、环境与安全

- 敏感配置只放在 `backend/.env`（已在 `.gitignore`）。
- 示例配置用 `backend/.env.example`，**占位符**命名（如 `CHANGE_ME`）。
- 生产容器地址：`NEPU_CONTAINER_PUBLIC_HOST`。
- 旧库比赛时间兼容：`NEPU_START_TIME_LEGACY_LOCAL=1`（见 `PUSH_CHANGELOG.md` 2026-09-17 说明）。

---

## 七、快速决策表

| 我要… | 先读 | 主要改 |
|--------|------|--------|
| 修 Flag/计分 | 功能说明书 §3.4 | `challenges.py`, `scoring_service.py`, `flag_generator.py` |
| 修容器 | 功能说明书 §3.5 | `container_service.py`, `challenges.py`, `container_access.py` |
| 修注册/登录 | 功能说明书 §3.1 | `auth.py`, `Auth.vue`, `maintenance_service.py` |
| 做管理后台 | 功能说明书 §3.9 | `platform_admin.py`, `admin/CtfManagement.vue` |
| 改页面样式 | AGENT.md + ui-style.md | `frontend/src/components/`, `assets/*.css` |
| 部署上线 | deploy-single-server.md | `deploy/`, `backend/.env` |

---

*最后更新：2026-09-17*
