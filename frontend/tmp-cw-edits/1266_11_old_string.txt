<style scoped>
.challenge-workspace { min-height: 400px; }
.workspace-header {
  display: flex;
  flex-direction: column;
  gap: 12px;
  margin-bottom: 12px;
}
.workspace-header--compact {
  margin-bottom: 16px;
}
.workspace-toolbar {
  display: flex;
  align-items: center;
  justify-content: space-between;
  gap: 12px;
  flex-wrap: wrap;
}
.workspace-head {
  flex: 1;
  min-width: 0;
  margin-bottom: 0;
  padding-bottom: 0;
  display: flex;
  align-items: flex-end;
  justify-content: space-between;
  gap: 12px;
  flex-wrap: wrap;
}
.header-admin-actions {
  flex-shrink: 0;
}
.challenge-workspace.admin-tools-on {
  outline: 1px solid rgba(217, 119, 6, 0.35);
  outline-offset: 4px;
  border-radius: var(--card-radius);
}
.admin-manage-hint {
  margin: 0 0 12px;
  padding: 8px 10px;
  font-size: 12px;
  color: var(--warning, #D97706);
  background: rgba(217, 119, 6, 0.08);
  border-radius: var(--radius-sm, 8px);
  line-height: 1.5;
}
.manage-section {
  margin-bottom: 16px;
  padding-bottom: 12px;
  border-bottom: 1px solid var(--border);
}
.manage-section:last-child { border-bottom: none; margin-bottom: 0; }
.manage-section-title {
  margin: 0 0 8px;
  font-size: 12px;
  font-weight: 700;
  letter-spacing: 0.04em;
  text-transform: uppercase;
  color: var(--muted);
}
.manage-actions {
  display: flex;
  flex-wrap: wrap;
  gap: 8px;
  margin-top: 10px;
}
.header-left {
  display: flex;
  align-items: center;
  gap: 10px;
  flex-wrap: wrap;
}
.header-left .matrix-page-title {
  margin: 0;
  font-size: 1.25rem;
}
.search-input { flex: 1; min-width: 180px; max-width: 320px; }
.category-tabs {
  display: flex;
  flex-wrap: wrap;
  gap: 6px;
  margin-bottom: 16px;
}
.workspace-tabs :deep(.n-tabs-rail) {
  padding: 3px;
  background: var(--hover);
  border-radius: var(--card-radius);
}
.workspace-tabs :deep(.n-tabs-tab) {
  font-size: 13px;
}
.workspace-tabs :deep(.n-tabs-pane-wrapper) {
  padding-top: 12px;
}
.cat-tab {
  padding: 4px 12px;
  border-radius: var(--card-radius);
  border: 1px solid var(--border);
  background: transparent;
  font-size: 13px;
  cursor: pointer;
  color: var(--muted);
  transition: all 0.15s;
}
.cat-tab.active {
  background: var(--primary);
  color: #fff;
  border-color: var(--primary);
}
.workspace-body {
  display: grid;
  grid-template-columns: 280px 1fr;
  gap: 16px;
  min-height: 420px;
}
.challenge-list {
  border: 1px solid var(--gradient-card-border, var(--border));
  border-radius: var(--card-radius);
  padding: 8px;
  max-height: calc(100vh - 280px);
  min-height: 420px;
  overflow-y: auto;
  background: var(--gradient-card-bg, var(--card-bg));
  box-shadow: var(--gradient-card-shadow, none);
}
.challenge-card {
  padding: 10px 12px;
  border-radius: var(--card-radius);
  cursor: pointer;
  margin-bottom: 6px;
  border: 1px solid var(--gradient-card-border, var(--border));
  background: var(--gradient-card-bg, var(--card-bg));
  box-shadow: 0 2px 8px rgba(0, 0, 0, 0.06);
  transition: border-color 0.2s, box-shadow 0.2s, transform 0.2s, background 0.2s;
}
.challenge-card:hover, .challenge-card.active {
  background: var(--hover);
  border-color: rgba(var(--primary-rgb), 0.45);
  box-shadow: var(--gradient-card-shadow-hover, var(--gradient-card-shadow));
  transform: translateY(-1px);
}
.challenge-card.solved .ch-title { color: var(--success, #18a058); }
.challenge-card.disabled { opacity: 0.55; }
.ch-title-row {
  display: flex;
  align-items: flex-start;
  justify-content: space-between;
  gap: 4px;
}
.admin-menu-btn {
  flex-shrink: 0;
  background: transparent;
  border: none;
  color: var(--muted);
  cursor: pointer;
  padding: 0 4px;
  font-size: 16px;
  line-height: 1;
}
.admin-menu-btn:hover { color: var(--primary); }
.admin-popover { display: flex; flex-direction: column; gap: 8px; min-width: 140px; }
.admin-popover__hint { margin: 0; font-size: 13px; color: var(--muted); }
.ch-title { font-weight: 600; font-size: 14px; flex: 1; }
.off-badge {
  font-size: 10px;
  color: var(--accent-red, #f83030);
  border: 1px solid var(--accent-red, #f83030);
  border-radius: var(--radius-sm);
  padding: 0 4px;
}
.ch-meta {
  display: flex;
  align-items: center;
  gap: 8px;
  margin-top: 4px;
  font-size: 12px;
  color: var(--muted);
}
.blood-tag {
  font-size: 10px;
  font-weight: 700;
  padding: 1px 5px;
  border-radius: 4px;
  line-height: 1.3;
}
.blood-tag--0 { color: #ef4444; background: rgba(239, 68, 68, 0.12); }
.blood-tag--1 { color: #f97316; background: rgba(249, 115, 22, 0.12); }
.blood-tag--2 { color: #eab308; background: rgba(234, 179, 8, 0.12); }
.blood-list { list-style: none; padding: 0; margin: 0; }
.blood-list-item {
  display: flex;
  flex-wrap: wrap;
  align-items: center;
  gap: 8px;
  padding: 8px 0;
  border-bottom: 1px solid var(--border);
  font-size: 13px;
}
.blood-time { margin-left: auto; font-size: 12px; }
.solved-mark { color: var(--success); }
.challenge-detail {
  border: 1px solid var(--gradient-card-border, var(--border));
  border-radius: var(--card-radius);
  padding: 16px;
  background: var(--gradient-card-bg, var(--card-bg));
  box-shadow: var(--gradient-card-shadow, none);
  display: flex;
  flex-direction: column;
  min-height: calc(100vh - 280px);
}
.challenge-workspace.mode-competition .workspace-body {
  grid-template-columns: minmax(260px, 320px) minmax(0, 1fr);
  gap: 20px;
}

.challenge-workspace.mode-competition .category-tabs {
  gap: 8px;
  margin-bottom: 20px;
}

.challenge-workspace.mode-competition .cat-tab {
  padding: 6px 16px;
  border-radius: 999px;
  background: var(--gradient-card-bg, var(--card-bg));
  box-shadow: var(--gradient-card-shadow, none);
  font-weight: 500;
  transition: border-color 0.2s, background 0.2s, color 0.2s, transform 0.2s;
}

.challenge-workspace.mode-competition .cat-tab:hover {
  border-color: rgba(var(--primary-rgb), 0.35);
  transform: translateY(-1px);
}

.challenge-workspace.mode-competition .cat-tab.active {
  background: var(--gradient-btn-fill, var(--primary));
  color: var(--on-primary-text, #fff);
  border-color: transparent;
  box-shadow: var(--gradient-btn-glow, none);
}

.challenge-workspace.mode-competition .workspace-toolbar {
  padding: 12px 16px;
  border: 1px solid var(--gradient-card-border, var(--border));
  border-radius: var(--card-radius);
  background: var(--gradient-card-bg, var(--card-bg));
  box-shadow: var(--gradient-card-shadow, none);
}

.mission-rail {
  padding: 0;
  overflow: hidden;
  display: flex;
  flex-direction: column;
}

.mission-rail-head {
  display: flex;
  align-items: center;
  justify-content: space-between;
  padding: 14px 16px 10px;
  border-bottom: 1px solid var(--border);
}

.mission-rail-title {
  font-size: 13px;
  font-weight: 700;
  color: var(--text);
}

.mission-rail-count {
  font-size: 12px;
  font-weight: 600;
  font-variant-numeric: tabular-nums;
  color: var(--muted);
  padding: 2px 8px;
  border-radius: 999px;
  background: var(--hover);
}

.mission-rail .challenge-card,
.mission-rail .empty {
  margin-left: 8px;
  margin-right: 8px;
}

.mission-rail .challenge-card:first-of-type {
  margin-top: 8px;
}

.mission-rail .empty {
  margin-top: 8px;
  margin-bottom: 8px;
}

.mission-card {
  position: relative;
  padding: 12px 12px 12px 16px;
  border-left: 3px solid var(--cat-stripe, var(--primary));
  margin-bottom: 8px;
}

.mission-card.active {
  background: rgba(var(--primary-rgb), 0.06);
  border-color: rgba(var(--primary-rgb), 0.35);
  border-left-color: var(--cat-stripe, var(--primary));
}

.mission-card.solved {
  border-left-color: var(--success, #16A34A);
}

.mission-card.solved .ch-title {
  color: var(--success, #16A34A);
}

.cat-chip {
  display: inline-flex;
  align-items: center;
  max-width: 120px;
  padding: 2px 8px;
  border-radius: 999px;
  font-size: 11px;
  font-weight: 600;
  line-height: 1.4;
  overflow: hidden;
  text-overflow: ellipsis;
  white-space: nowrap;
}

.cat-chip--lg {
  max-width: none;
  font-size: 12px;
  padding: 4px 10px;
}

.ch-points {
  font-family: 'JetBrains Mono', 'Fira Code', ui-monospace, monospace;
  font-size: 12px;
  font-weight: 600;
  font-variant-numeric: tabular-nums;
  color: var(--primary);
}

.mission-detail {
  padding: 0;
  overflow: hidden;
}

.mission-detail .detail-splitter {
  padding: 0 16px 16px;
}

.mission-detail .detail-intro {
  padding-top: 16px;
}

.mission-detail .detail-header h3 {
  font-size: 1.15rem;
  font-weight: 700;
}

.empty-detail-icon {
  font-size: 36px;
  margin-bottom: 8px;
  opacity: 0.85;
}

.challenge-workspace.mode-competition .empty-detail {
  gap: 4px;
  font-size: 14px;
}

@media (prefers-reduced-motion: reduce) {
  .challenge-workspace.mode-competition .cat-tab:hover,
  .mission-card:hover {
    transform: none;
  }
}

.challenge-workspace.mode-training .workspace-header {
  padding-bottom: 16px;
  margin-bottom: 16px;
  border-bottom: 1px solid var(--border);
}
.challenge-workspace.mode-training .cat-tab {
  background: var(--gradient-card-bg, var(--card-bg));
  box-shadow: inset 0 1px 0 rgba(255, 255, 255, 0.04);
}
.detail-splitter {
  flex: 1;
  min-height: 380px;
  height: 100%;
}
.detail-intro {
  height: 100%;
  overflow-y: auto;
  padding: 0 4px 8px;
  box-sizing: border-box;
}
.detail-bottom {
  height: 100%;
  min-height: 0;
  overflow: hidden;
  display: flex;
  flex-direction: column;
}
.detail-bottom :deep(.n-tabs) { flex: 1; display: flex; flex-direction: column; }
.detail-bottom :deep(.n-tab-pane) { flex: 1; overflow-y: auto; }
.empty-detail {
  align-items: center;
  justify-content: center;
  color: var(--muted);
}
.detail-header {
  display: flex;
  align-items: baseline;
  gap: 12px;
  margin-bottom: 8px;
}
.detail-header h3 { margin: 0; }
.solve-count { font-size: 13px; color: var(--muted); }
.tag-row { display: flex; gap: 6px; margin-bottom: 8px; flex-wrap: wrap; }
.detail-desc {
  margin-bottom: 12px;
  line-height: 1.7;
  font-size: 14px;
  flex-shrink: 0;
  max-height: 200px;
  overflow-y: auto;
}
.detail-desc :deep(pre) {
  background: var(--code-bg, #f4f4f5);
  padding: 12px;
  border-radius: var(--radius-md);
  overflow-x: auto;
}
.attachment-row {
  display: flex;
  align-items: center;
  gap: 8px;
  margin-bottom: 12px;
  font-size: 14px;
}
.attachment-row a { color: var(--primary); }
.terminal-panel { padding: 4px 0; }
.terminal-hint {
  font-size: 12px;
  color: var(--muted);
  margin-bottom: 12px;
  padding: 8px 10px;
  background: var(--code-bg);
  border-radius: var(--radius-md);
  border-left: 3px solid var(--primary);
}
.container-section { margin-bottom: 12px; }
.container-toolbar {
  display: flex;
  align-items: center;
  justify-content: space-between;
  margin-bottom: 8px;
}
.file-label { font-family: monospace; font-size: 13px; color: var(--primary); }
.conn-box {
  padding: 10px 12px;
  background: var(--term-bg, #0d1117);
  color: var(--term-fg, #58a6ff);
  border-radius: var(--radius-md);
  font-family: monospace;
  font-size: 13px;
  word-break: break-all;
  display: flex;
  align-items: center;
  gap: 8px;
  flex-wrap: wrap;
}
.tcp-badge {
  font-size: 10px;
  padding: 1px 6px;
  border: 1px solid var(--term-fg);
  border-radius: var(--radius-sm);
}
.flag-bar {
  display: flex;
  gap: 8px;
  margin-top: 12px;
  padding-top: 12px;
  border-top: 1px solid var(--border);
}
.flag-bar :deep(.n-input) { flex: 1; }
.hint-list { list-style: none; margin: 0; padding: 0; }
.hint-item {
  padding: 10px 0;
  border-bottom: 1px solid var(--border);
}
.hint-item:last-child { border-bottom: none; }
.hint-head { display: flex; align-items: center; gap: 8px; margin-bottom: 4px; }
.penalty-tag { font-size: 11px; color: var(--accent-red, #f83030); }
.hint-text { margin: 0; line-height: 1.7; font-size: 14px; color: var(--text); }
.muted { color: var(--muted); }
.empty { text-align: center; padding: 24px; color: var(--muted); }
.admin-panel { display: flex; flex-direction: column; gap: 10px; font-size: 14px; }
.admin-link { color: var(--primary); font-size: 13px; }
.admin-stats .stat-row, .admin-panel .stat-row {
  display: flex;
  justify-content: space-between;
  padding: 8px 0;
  border-bottom: 1px solid var(--border);
}
.admin-stats .stat-row:last-child, .admin-panel .stat-row:last-child { border-bottom: none; }
.mono { font-family: monospace; font-size: 12px; word-break: break-all; }
@media (max-width: 900px) {
  .workspace-body { grid-template-columns: 1fr; }
  .challenge-list { max-height: 240px; }
}
</style>